# Minimal inference runtime for the pinned TrackNetV4 adapter on Apple silicon.
# The upstream environment.yml includes Linux-only CUDA packages and cannot be used on macOS.
tensorflow==2.17.0
tensorflow-metal==1.1.0
keras==3.5.0
numpy==1.26.4
opencv-python==4.10.0.84
pillow==10.4.0
